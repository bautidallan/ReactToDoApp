const Title=({titleName})=>{
    return (
        <header className="flex justify-center items-center m-8"> 
            <h1 className="font-mono font-bold text-4xl ">{titleName}</h1>
        </header>
    )
}
export default Title